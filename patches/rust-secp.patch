diff --git a/src/key/mod.rs b/src/key/mod.rs
index b80315b8..a905c5bd 100644
--- a/src/key/mod.rs
+++ b/src/key/mod.rs
@@ -1728,6 +1728,7 @@ mod test {
         for _ in 0..COUNT {
             let (_, pk) = crate::test_random_keypair();
             let hash = hash(&pk);
+            println!("Generated key {pk} hash {hash}");
             assert!(!set.contains(&hash));
             set.insert(hash);
         }
diff --git a/src/lib.rs b/src/lib.rs
index bcd3b2bf..98a3a876 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -517,9 +517,9 @@ fn test_random_32_bytes() -> [u8; 32] {
 
     let mut ret = [0; 32];
     for i in 0..8 {
-        let rng = RNG.load(Ordering::Relaxed).wrapping_mul(PRIME_1).wrapping_add(PRIME_2);
+        let rng = RNG.load(Ordering::SeqCst).wrapping_mul(PRIME_1).wrapping_add(PRIME_2);
         ret[i * 4..(i + 1) * 4].copy_from_slice(&rng.to_be_bytes());
-        RNG.store(rng, Ordering::Relaxed);
+        RNG.store(rng, Ordering::SeqCst);
     }
     ret
 }
